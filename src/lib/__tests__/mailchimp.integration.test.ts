import { describe, it } from 'vitest';

describe('Mailchimp integration (skipped)', () => {
  it('placeholder', () => {
    // Tests intentionally skipped; mailchimp integration is not implemented yet.
  });
});
